---
tags: [registro]
data: 2026-09-15
---

# 2026-09-15

Voltar ao [[00 Índice]].

## O que foi feito
1. **Revisão antes do GitHub:** busca por chaves de API e dados pessoais em todos os arquivos que entram no repositório, conferência do relatório e das fotos do horário, `tsc`, `eslint`, `next build` e `npm audit`. Nada encontrado ([[DEC-017 Repositório público no GitHub]]).
2. **Ajustes no repositório:** `.gitignore` passou a ignorar os plugins do Obsidian, arquivos locais do Supabase CLI e `.claude/settings.local.json`; `.gitattributes` criado com LF; SVGs padrão do Next removidos de `public/`.
3. **Projeto virou TCC:** orientador do TCC é o Prof. Celso de Ávila Ramos, e a Profª. Flávia continua citada como orientadora da IC. Atualizados o [[README]], a [[Visão geral]] e a seção de equipe da página do projeto (`lib/project-content.ts` e `app/page.tsx`).
4. **Repositório criado:** público, sem licença, commits com o e-mail noreply do GitHub.
5. **Claude fora do histórico:** o Lucca reescreveu as mensagens dos dois primeiros commits para tirar a linha de coautoria do Claude; o GitHub passou a listar só `LuccaMV` como contribuidor.
6. **`.claude/` fora do Git:** a pasta deixou de ser versionada e entrou no `.gitignore`. O `launch.json` continua no computador do Lucca.
7. **Primeiro deploy na Vercel:** o chat publicado respondia "Erro interno ao processar a mensagem". O log mostrou `GEMINI_CHAT_MODEL` e `PROMPT_VERSION` cadastradas vazias no painel: o valor padrão do zod só vale quando a variável não existe, e "" falhava no `min(1)`. `lib/server/env.ts` passou a tratar variável vazia como ausente, então ela usa o padrão (ou continua opcional).

## Verificações
- Nenhum padrão de chave (Gemini, Groq, Supabase, JWT, chave privada) nos arquivos versionados; `.env.local` fora do Git.
- `tsc --noEmit`, `eslint` e `next build` passando depois das mudanças.

## Aprendizados
- Na Vercel, variável cadastrada sem valor chega ao código como texto vazio, não como ausente.
- O Git desta máquina não tinha nome e e-mail configurados; a identidade foi definida só no repositório, não globalmente.
- O Git está com `core.autocrlf=true`; o `.gitattributes` evita que o repositório receba CRLF.

## Próximos passos
- Respostas do Lucca no mesmo dia: o Rafael continua como coautor; o papel da Profª. Flávia no TCC ainda não foi definido, mas ela continua citada; a troca de "Iniciação Científica" por "TCC" nos textos fica para depois ([[Pendências]]).
- Lucca: definir o papel da Profª. Flávia, as datas do TCC e a licença ([[Pendências]]).
- Lucca: adicionar o Rafael e os orientadores como colaboradores, se quiser que eles editem.
- Claude: limite de requisições por IP em `/api/chat` antes do deploy público na Vercel.
